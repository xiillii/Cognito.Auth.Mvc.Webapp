@{
    ViewData["Title"] = "Protected Page";
}

<p>Claims information of the session</p>

<table class="table table-striped table-bordered">
    @foreach (var claim in User.Claims)
    {
        <tr>
            <td>@claim.Type</td>
            <td>@claim.Value</td>
        </tr>
    }
</table>